# Numbat mobile app

A mobile app for [Numbat](https://numbat.dev).

## Developer notes

```bash
# Run locally
cargo tauri dev

# Build APK:
./build.sh

# Build AAB (Android App Bundle) for Play Store:
./build.sh --bundle
```
